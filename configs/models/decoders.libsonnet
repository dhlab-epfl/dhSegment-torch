{
    unet: {
      type: "unet",
      decoder_channels: error 'Must set "decoder channels"',
      max_channels: 512
    },
    pan: {
      type: "pan",
      decoder_channels_size: error 'Must set "decoder channels size"',
    },

}